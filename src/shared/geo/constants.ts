export const METERS_PER_DEGREE = 111_320;
export const MIN_COSINE = 0.1;

/** Default fetch-bounds padding multiplier applied around the poster area. */
export const FETCH_PADDING = 1.35;
/** Minimum safe fetch/poster radius in meters to avoid degenerate queries. */
export const MIN_SAFE_DISTANCE_METERS = 1_000;
/** Minimum safe aspect ratio to avoid degenerate projections. */
export const MIN_SAFE_ASPECT_RATIO = 0.2;

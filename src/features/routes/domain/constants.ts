export const MIN_GPX_STROKE_WIDTH = 1;
export const MAX_GPX_STROKE_WIDTH = 12;
export const DEFAULT_GPX_STROKE_WIDTH = 3;

export const MIN_GPX_OPACITY = 0.1;
export const MAX_GPX_OPACITY = 1;
export const DEFAULT_GPX_OPACITY = 1;

export const DEFAULT_GPX_COLOR = "#e54a2f";

export const GPX_DASH_PATTERN: [number, number] = [3, 2];

export const GPX_LINE_STYLES = ["solid", "dashed"] as const;

export const MAX_GPX_FILE_SIZE_BYTES = 10 * 1024 * 1024; // 10 MB
